# Ubuntu 24.04 Server LPE Hunt Archive

Preserved from `/Users/andrewmohawk/Documents/UbuntuLPE/ubuntu24-server-lpe` so the research tree is not lost.

This directory contains the working notes, negative findings, baselines, logs, source-audit material, and probe scripts from the Ubuntu 24.04 server LPE hunt. Generated Python bytecode and `.DS_Store` files were intentionally omitted.

The extra `session-tools/` directory preserves root-level helper scripts from `/Users/andrewmohawk/Documents/UbuntuLPE/tools` that were not inside the `ubuntu24-server-lpe` tree.

